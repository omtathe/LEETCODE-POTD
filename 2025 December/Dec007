class Solution {
    public int countOdds(int low, int high) {
        int diff = high - low;

        // If either low or high is odd → count one more
        if (low % 2 == 1 || high % 2 == 1) {
            return diff / 2 + 1;
        } else {
            return diff / 2;
        }
    }
}
